下面这段可以直接作为 **Claude Code 的 `/goal` 提示词** 使用。

---

# /goal

请严格按照仓库中的权威规格文档 [文档2.MD](file:///d:/Code_MyProject/RAG_Sec/文档2.MD) 完成 **Phase 0：项目初始化** 的开发，并在完成后停止，等待下一步指令。

## 一、必须先读的文件

在开始任何实现前，必须完整阅读并严格遵循以下文件：

- [文档2.MD](file:///d:/Code_MyProject/RAG_Sec/文档2.MD)  
  这是**唯一权威实现规格**，所有实现决策必须可追溯到该文档。
- [CLAUDE.md](file:///d:/Code_MyProject/RAG_Sec/CLAUDE.md)  
  这里补充了仓库当前状态、执行纪律、默认决策表与实现边界。

可选参考但**不作为契约**：
- [文档.md](file:///d:/Code_MyProject/RAG_Sec/文档.md)

---

## 二、当前任务范围：只实现 Phase 0

只允许实现 [文档2.MD](file:///d:/Code_MyProject/RAG_Sec/文档2.MD#L2160-L2193) 中定义的 **Phase 0** 内容，**不得提前实现 Phase 1 及之后的业务功能**。

### 本次必须完成的内容
根据 Phase 0 要求，完成以下事项：

1. 创建项目基础目录结构
2. 创建 FastAPI 应用
3. 创建 `docker-compose.yml`
4. 集成 PostgreSQL
5. 集成 Redis
6. 集成 Qdrant
7. 创建 `.env.example`
8. 创建 `.gitignore`，并忽略 `models/`
9. 创建或完善 `README`
10. 实现 `GET /health`

---

## 三、禁止提前实现的内容

本次 **绝对不要** 提前实现以下内容：

- 不要实现任何 RAG 业务逻辑
- 不要实现数据库业务模型与 Alembic migration
- 不要实现上传、解析、chunk、embedding、rerank、retrieval、query、eval 等后续模块
- 不要实现 OpenSearch
- 不要实现前端
- 不要实现 LangGraph
- 不要实现复杂 OCR
- 不要实现任何超出 Phase 0 的“顺手优化”

如果某些后续模块的目录结构在 Phase 0 必须预留，可以只创建**最小占位文件**并写清楚 `TODO`，但不要补业务实现。

---

## 四、严格遵循的技术与结构要求

### 1. 工程结构
项目结构必须与 [文档2.MD](file:///d:/Code_MyProject/RAG_Sec/文档2.MD#L413-L533) 的目录设计保持一致。  
允许在 Phase 0 范围内先创建最小可运行骨架。

### 2. 技术栈
必须使用：

- Python 3.11+
- FastAPI
- Pydantic v2
- PostgreSQL
- Redis
- Qdrant
- Docker Compose
- `pyproject.toml` 管理依赖

### 3. 配置要求
- 所有配置从 `.env` 读取
- 生成 `.env.example`
- `models/` 必须加入 `.gitignore`
- 不得把本地模型文件提交进仓库

### 4. 代码风格要求
- **所有你写的代码必须带完整中文注释**
- 关键模块、关键配置、关键函数、关键启动逻辑都要有**教学型中文注释**
- 注释目标是“方便初学者学习这个项目为什么这么写”
- 类型标注必须补齐
- API 层不要写业务逻辑
- 日志与错误处理要清晰，但不要超范围扩展

---

## 五、测试要求：必须做“完整的功能测试”

你必须在 Phase 0 完成后，补齐并执行**完整的功能测试与验收测试**，至少包括以下内容：

### 1. 健康检查接口测试
验证：

- `GET /health` 返回 200
- 返回体包含：
  ```json
  {
    "status": "ok"
  }
  ```

### 2. 基础服务联通性验证
验证在 `docker compose up` 后：

- FastAPI 服务可以启动
- PostgreSQL 服务可以连接
- Redis 服务可以连接
- Qdrant 服务可以连接

### 3. 至少提供以下测试成果
你需要实际补上并运行：

- 基础自动化测试（例如 `pytest`）
- 至少一个健康检查接口测试
- 至少一个配置/启动层面的基本验证
- 必要时可增加最小集成测试

### 4. 验收必须可复现
最终必须给出清晰的：

- 启动命令
- 测试命令
- 验收步骤
- 预期结果

---

## 六、文档要求：必须产出“供学习的实现文档”

除了代码本身，你还必须补一份**中文实现文档**，供学习使用。要求如下：

### 文档目标
让我能看懂：

- Phase 0 做了什么
- 为什么这么设计
- 每个关键文件的作用
- 如何启动
- 如何测试
- 如何验证环境联通
- 当前哪些内容只是 Phase 0 占位，后面 Phase 再实现

### 文档要求
- 使用中文
- 讲解要面向学习者
- 不要只写结论，要解释关键设计原因
- 可以放在 README 中，也可以补充单独文档
- 如果拆成单独文档，文件名请使用中文且语义明确

---

## 七、执行步骤要求

请严格按下面顺序执行：

### Step 1：先说明计划
先明确告诉我本次你准备修改/新增哪些文件，并说明每个文件的作用。

### Step 2：实现代码
只实现 Phase 0 范围内的代码与工程文件。

### Step 3：补充中文注释
确保你新增或修改的关键代码有完整中文注释。

### Step 4：补充测试
编写并运行 Phase 0 所需测试。

### Step 5：补充中文实现文档
写出供学习使用的实现说明。

### Step 6：运行并验证
你必须实际执行必要命令验证项目可运行，并验证测试通过。

### Step 7：最终交付时必须输出
最终请按以下结构汇报结果：

#### 1. 本次修改/新增文件清单
逐项列出文件及用途

#### 2. 已实现内容
说明 Phase 0 已完成哪些要求

#### 3. 测试与验证结果
列出实际执行过的命令与结果，包括：

- 安装/启动命令
- 测试命令
- 服务联通验证命令
- 验收结果

#### 4. 使用说明
告诉我如何在本地复现启动与测试

#### 5. 学习说明
指出最值得我阅读的几个核心文件

#### 6. 停止
完成 Phase 0 后**必须停止**，不要继续做 Phase 1

---

## 八、Phase 0 验收标准（必须全部满足）

你最终交付时，必须确认以下全部满足：

- `docker compose up` 可以启动
- `GET /health` 返回 `ok`
- 数据库连接正常
- Redis 连接正常
- Qdrant 连接正常
- 有完整中文注释
- 有完整中文实现文档
- 有完整功能测试与验证说明
- 完成后停止，不进入下一阶段

---

## 九、额外实现偏好

请按以下偏好执行：

- 优先写**清晰、教学友好、易读**的代码
- 不要为了“高级感”引入多余抽象
- 不要过度设计
- 对初学者重要的地方，请写更详细的中文注释
- README/实现文档中尽量解释：
  - 为什么 Phase 0 只做这些
  - 为什么此时不做 RAG 逻辑
  - Docker 中每个服务的职责
  - 后续 Phase 将如何在当前骨架上扩展

---

现在开始执行：  
**只完成 Phase 0，并完成完整功能测试、中文注释、中文实现文档。完成后停止。**

---

如果你愿意，我还能继续帮你再做一版：

1. **更强硬约束版 `/goal`**（更适合防止它超范围实现）  
2. **更教学版 `/goal`**（更强调注释、README 和学习文档）  
3. **更工程版 `/goal`**（更强调测试、docker、目录、可运行性）

你要的话我可以直接再给你一版“更适合 Claude Code 执行”的增强提示词。