# Alembic 配置占位文件。
#
# 注意：Phase 0 不实现数据库业务模型与迁移（规格 Phase 1 范围）。
# 本文件仅为预留骨架，Phase 1 将在 backend/alembic/ 下生成 env.py 与版本目录，
# 并填充 script_location 等真实配置后启用 alembic upgrade head。
#
# TODO(Phase 1): 配置 Alembic 迁移环境。

[alembic]
# 占位：Phase 1 改为 backend/alembic
script_location = backend/alembic
# 占位：Phase 1 从 .env 读取
sqlalchemy.url =
